/**
 * Analytics v2 - Public API Export
 * 
 * Main entry point for analytics.
 * Export the singleton instance and types.
 */

export { analytics } from './Analytics';
export type { AnalyticsEvent, AnalyticsConfig, QueueStats } from './types';
export type { AnalyticsProvider } from './types';





